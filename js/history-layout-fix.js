/* PRIANGAN MULTIMEDIA — history actions below quotation data */
(function(){
  'use strict';
  const S=v=>String(v??'').trim();
  const getId=el=>{const m=S(el?.getAttribute('onclick')).match(/(?:editQuotation|publishQuotation|deleteQuotation|inputDP|inputPelunasan)\s*\(\s*(\d+)\s*\)/);return m?Number(m[1]):null;};
  function normalizePaymentColumns(table){const head=table.querySelector('thead tr');if(!head)return;const headers=[...head.children],seen={},remove=[];headers.forEach((th,i)=>{const key=S(th.textContent).toUpperCase();if(key==='DP'||key==='DIBAYAR'){seen[key]=(seen[key]||0)+1;if(seen[key]>1)remove.push(i);}});remove.reverse().forEach(i=>{head.children[i]?.remove();table.querySelectorAll('tbody tr').forEach(tr=>tr.children[i]?.remove());});}
  function findOriginalActionCell(tr){return [...tr.children].find(td=>td.querySelector('[onclick*="editQuotation"],[onclick*="publishQuotation"],[onclick*="deleteQuotation"],[onclick*="inputDP"],[onclick*="inputPelunasan"]'));}
  function buildButton(className,label,handler){const b=document.createElement('button');b.type='button';b.className=className;b.textContent=label;if(handler)b.addEventListener('click',handler);return b;}
  function normalizeActions(table){const tbody=table.querySelector('tbody');if(!tbody)return;[...tbody.querySelectorAll('tr')].forEach(tr=>{if(tr.dataset.pmActionRow==='1')return;const actionCell=findOriginalActionCell(tr);if(!actionCell)return;const buttons=[...actionCell.querySelectorAll('button')],edit=buttons.find(b=>/edit/i.test(b.textContent)),publish=buttons.find(b=>/publish/i.test(b.textContent)),del=buttons.find(b=>/hapus/i.test(b.textContent)),dp=buttons.find(b=>/^dp$/i.test(b.textContent)),paid=buttons.find(b=>/bayar/i.test(b.textContent));const id=getId(edit)||getId(publish)||getId(del)||getId(dp)||getId(paid);if(!id)return;actionCell.remove();const next=tr.nextElementSibling;if(next?.dataset.pmActionRow==='1')return;const actionRow=document.createElement('tr');actionRow.dataset.pmActionRow='1';actionRow.className='pmHistoryActionRow';const actionTd=document.createElement('td');actionTd.colSpan=Math.max(1,tr.children.length);actionTd.className='pmHistoryActionCell';const wrap=document.createElement('div');wrap.className='pmHistoryActions';if(edit)wrap.appendChild(buildButton('btn sm','Edit',()=>window.editQuotation?.(id)));if(publish)wrap.appendChild(buildButton('btn green sm','Publish',()=>window.publishQuotation?.(id)));else{const sent=document.createElement('span');sent.className='pmAlreadySent';sent.textContent='Sudah diberikan';wrap.appendChild(sent);}if(dp)wrap.appendChild(buildButton('btn secondary sm','DP',()=>window.inputDP?.(id)));if(paid)wrap.appendChild(buildButton('btn green sm','Bayar',()=>window.inputPelunasan?.(id)));if(del)wrap.appendChild(buildButton('btn red sm','Hapus',()=>window.deleteQuotation?.(id)));actionTd.appendChild(wrap);actionRow.appendChild(actionTd);tr.insertAdjacentElement('afterend',actionRow);});}
  function apply(){if(!/^Riwayat Penawaran$/i.test(S(document.querySelector('#title')?.textContent)))return;document.querySelectorAll('#content table').forEach(table=>{normalizePaymentColumns(table);normalizeActions(table);table.classList.add('pm-history-below-actions');});}
  const style=document.createElement('style');style.textContent=`.pm-history-below-actions{width:100%;table-layout:auto}.pm-history-below-actions th,.pm-history-below-actions td{vertical-align:middle}.pmHistoryActionRow td.pmHistoryActionCell{padding:8px 10px!important;border-top:0!important;background:rgba(30,41,59,.32)!important}.pmHistoryActions{display:flex!important;flex-direction:row!important;align-items:center!important;justify-content:flex-start!important;gap:6px!important;flex-wrap:wrap!important;width:100%!important}.pmHistoryActions .btn.sm{padding:6px 10px!important;font-size:12px!important;line-height:1!important;margin:0!important;white-space:nowrap!important}.pmAlreadySent{font-size:11px!important;color:#7dd3fc!important;padding:6px 4px!important;white-space:nowrap!important}@media(max-width:800px){.pmHistoryActions{gap:5px!important}}`;document.head.appendChild(style);
  /* Bounded passes only. No global MutationObserver: it was causing repeated DOM work and page freezes when entering history. */
  setTimeout(apply,150);setTimeout(apply,600);setTimeout(apply,1500);
})();
