/* TTD final retry: wait for preview DOM, fetch template again, and re-inject signature. */
(function(){
  'use strict';
  const S=v=>String(v??'').trim();
  function DB(){if(typeof db!=='undefined'&&db)return db;const c=window.PRIANGAN_CONFIG||{};const u=S(localStorage.getItem('SUPABASE_URL')||c.SUPABASE_URL),k=S(localStorage.getItem('SUPABASE_ANON_KEY')||c.SUPABASE_ANON_KEY);return u&&k&&window.supabase?.createClient?window.supabase.createClient(u,k):null;}
  async function template(){const d=DB();if(!d)return{};const r=await d.from('template_surat').select('*').order('id',{ascending:false}).limit(1).maybeSingle();if(!r.error&&r.data)return r.data;try{return JSON.parse(localStorage.getItem('PRIANGAN_TEMPLATE_BACKUP')||'{}')}catch(_){return{}}}
  async function url(v){const raw=S(v);if(!raw)return'';if(/^(https?:|data:|blob:)/i.test(raw))return raw;const d=DB();if(!d)return raw;const parts=raw.split('/').filter(Boolean);const candidates=[];if(parts.length>1)candidates.push([parts[0],parts.slice(1).join('/')]);for(const bucket of ['surat-assets','templates','assets'])candidates.push([bucket,raw]);for(const [bucket,path] of candidates){try{const pub=d.storage.from(bucket).getPublicUrl(path);if(pub?.data?.publicUrl)return pub.data.publicUrl;}catch(_){}try{const signed=await d.storage.from(bucket).createSignedUrl(path,3600);if(!signed.error&&signed.data?.signedUrl)return signed.data.signedUrl;}catch(_){} }return raw;}
  async function apply(){const o=document.getElementById('pmPrintPreview');if(!o)return;const t=await template();const u=await url(t.ttd_url);if(!u)return;let box=o.querySelector('.pm-signature-box');if(!box){box=o.querySelector('.pm-signature');}if(!box)return;let img=box.querySelector('img.signature');if(!img){img=document.createElement('img');img.className='signature';img.alt='Tanda tangan';const line=box.querySelector('.pm-signature-line');box.insertBefore(img,line||box.firstChild);}img.src=u;img.style.display='block';img.style.visibility='visible';img.style.opacity='1';img.onerror=()=>console.warn('TTD URL tidak dapat dimuat:',u);const name=box.querySelector('strong');const role=box.querySelector('.pm-signature-role');if(name)name.textContent=S(t.nama_penandatangan)||name.textContent;if(role)role.textContent=S(t.jabatan_penandatangan)||role.textContent;}
  let timer=null;const observer=new MutationObserver(()=>{if(document.getElementById('pmPrintPreview')){clearTimeout(timer);timer=setTimeout(apply,80);}});observer.observe(document.body,{childList:true,subtree:true});window.pmRetryTTD=apply;
})();
